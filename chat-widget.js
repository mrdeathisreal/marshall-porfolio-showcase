/*!
 * ============================================================
 * THIS FILE IS WITHHELD FROM THE PUBLIC SHOWCASE.
 * ============================================================
 * The full Marshall Chat Widget — drop-in i18n-aware chat
 * component that proxies prompts through a private Cloud
 * Function and styles itself to the host page — is part of the
 * private source repository.
 *
 * What you would see in the full file:
 *   • i18n module (Vietnamese, English, Chinese, Russian)
 *   • Auto-language detection from `navigator.language`
 *   • Streaming response handler tied to a Gemini-backed
 *     Cloud Function endpoint
 *   • Theming via `data-accent` data-attribute on the script tag
 *   • Two operating modes: `portfolio` (Q&A about Marshall) and
 *     `bds` (real-estate consulting) — same widget, different
 *     system prompt
 *   • Persistent conversation state in `localStorage`
 *
 * If you are evaluating this codebase for a hiring decision and
 * want to read the full file, contact:
 *
 *     nhhuy130@gmail.com
 *
 * See LICENSE — All Rights Reserved.
 * ============================================================
 */
(function () {
  'use strict';
  console.warn(
    '[Marshall Chat] chat-widget.js is withheld from the public showcase. ' +
      'Contact nhhuy130@gmail.com for licensed access.',
  );
})();
